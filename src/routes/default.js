import path from 'path';
import fs from 'fs';

import rootname from '../../rootname';

/**
 * default route
 *
 * @param {array} data
 * @return {function} Default function
 */
async function default(data) {

  return async function (req, res) {
      
  };

}

export default default;
